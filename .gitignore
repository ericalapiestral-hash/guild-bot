node_modules/
.env
config.json
*.zip

# 노션에서 읽어온 것들 — 만료되는 이미지 URL이 들어있고 봇이 알아서 다시 만든다
data/notion-dump.md
data/builds.json

# 음성 체류시간 기록 (봇이 쌓는 실데이터)
data/voice.json

# 오버레이 앱
overlay/node_modules/
overlay/.cache/
overlay/dist*/
*.exe

# 안드로이드 앱 (APK는 GitHub Actions에서 만든다)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
*.apk
